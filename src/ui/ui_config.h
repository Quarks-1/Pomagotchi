#pragma once

// Global UI configuration values
namespace UIConfig {
    // Corner radius for all UI elements
    constexpr uint8_t CORNER_RADIUS = 3;
    
    // Button dimensions
    constexpr int BUTTON_WIDTH = 80;
    constexpr int BUTTON_HEIGHT = 30;
    
    // Status bar dimensions
    constexpr int STATUS_BAR_HEIGHT = 25;
    constexpr int STATUS_BAR_WIDTH = 120;
    
    // Font sizes
    constexpr int TITLE_FONT_SIZE = 2;
    constexpr int STATUS_FONT_SIZE = 1;
} 